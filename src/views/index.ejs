<!DOCTYPE html>
<html lang="en">
  <%- include('partials/head') %>
  <body>
    <%- include('partials/nav') %>

    <p>All games:</p>

    <ul>
      <% games.forEach(game => { %>
      <li><a href="/games/<%= game.game_id %>"><%= game.title %></a></li>
      <% }) %>
    </ul>

    <!-- <%- include('partials/footer') %> -->
  </body>
</html>
